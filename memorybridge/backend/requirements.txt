# MemoryBridge — Combined requirements (Phase 1 + Phase 2)

# Shared
sentence-transformers>=3.0
sqlite-vec>=0.1.0

# Phase 1 — Flask embedder server
flask>=3.0
flask-cors>=4.0

# Phase 2 — FastAPI retrieval API
fastapi>=0.115
uvicorn[standard]>=0.30
google-auth>=2.30
google-api-python-client>=2.130
python-dotenv>=1.0
pydantic>=2.0
httpx>=0.27

# Process manager
supervisor>=4.2
